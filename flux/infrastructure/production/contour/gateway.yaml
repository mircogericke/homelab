apiVersion: gateway.networking.k8s.io/v1beta1
kind: Gateway
metadata:
  name: http-redirect
  namespace: projectcontour
spec:
  gatewayClassName: contour
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: All
